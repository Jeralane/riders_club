<%= form_with(model: chapter, local: true) do |form| %>
  <% if chapter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chapter.errors.count, "error") %> prohibited this chapter from being saved:</h2>

      <ul>
      <% chapter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :area %>
    <%= form.text_field :area, id: :chapter_area %>
  </div>

  <div class="field">
    <%= form.label :club_id %>
    <%= form.text_field :club_id, id: :chapter_club_id %>
  </div>

  <div class="field">
    <%= form.label :main %>
    <%= form.check_box :main, id: :chapter_main %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
